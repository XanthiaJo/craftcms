// main.js - App bootstrap, stage setup
// TODO: Initialize Konva stage, layers, and wire up event handlers

import '../css/app.css';
import { AppStage } from './konva/AppStage.js';
import { Store } from './state/Store.js';

// Initialize the store
const store = new Store();

// Initialize the Konva stage
// TODO: Create AppStage instance and mount to #konva-stage

console.log('KnitStichGrid Web - Placeholder loaded');
