// AppStage.js - Stage + layer composition + event wiring
// TODO: Create Konva.Stage, add layers (grid, overlay, sketch), wire events

export class AppStage {
  // TODO: Implement stage initialization and layer composition
}
