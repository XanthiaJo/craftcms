{% extends '_layouts/base.twig' %}

{% block pageTitle %}
    Home - {{ parent() }}
{% endblock %}

{% block pageContent %}
{% set home = craft.app.globals.getSetByHandle('home') %}
{% set highlightEntries = craft.entries.section('posts').type('post').with(['featuredImage']).orderBy('postDate desc').limit(3).all() %}
{% set heroEntry = highlightEntries[0] ?? null %}
{% set homeHeading = home and home.homeHeading ? home.homeHeading : '' %}
{% set homeIntro = home and home.homeIntro ? home.homeIntro : '' %}
{% set homeCards = home ? home.homeCards.all() : [] %}
{% set heroImage = heroEntry and heroEntry.featuredImage ? heroEntry.featuredImage.one() : null %}

<section class="home-hero"{% if heroImage %} style="--home-hero-image: url('{{ heroImage.url }}');"{% endif %}>
    <div class="container home-hero-inner">
        <h1>Hello There!</h1>
        <p class="subtitle">I'm Karima</p>
    </div>
</section>

<main class="home-main">
    <div class="container">
        <section class="home-intro">
            <h2>{{ homeHeading }}</h2>
            <p>{{ homeIntro }}</p>
        </section>

        <section class="home-project-grid" aria-label="Project highlights">
            {% if homeCards|length %}
                {% for card in homeCards %}
                    {% set cardImageField = card.getFieldValue('homeCardImage') %}
                    {% set cardImage = cardImageField ? cardImageField.one() : null %}
                    {% set cardText = card.getFieldValue('homeCardText') %}
                    {% set cardLink = card.getFieldValue('homeCardLink') %}
                    {% if cardLink %}
                        <a class="home-project-card-link" href="{{ cardLink }}">
                    {% endif %}
                    <article class="card card--image-top">
                        {% if cardImage %}
                            <img class="thumb" src="{{ cardImage.url }}" alt="{{ cardImage.title ?: card.title }}">
                        {% endif %}
                        <div class="card-body">
                            <h3>{{ card.title }}</h3>
                            {% if cardText %}
                                <p class="body">{{ cardText }}</p>
                            {% endif %}
                        </div>
                    </article>
                    {% if cardLink %}
                        </a>
                    {% endif %}
                {% endfor %}
            {% endif %}
        </section>

        <div style="text-align: center">
            <a class="button button-primary" href="/posts">See all projects!</a>
        </div>
    </div>
</main>
{% endblock %}